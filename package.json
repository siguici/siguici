{
  "name": "sigui.ci",
  "description": "Sigui's web application.",
  "engines": {
    "bun": ">=1.0.7",
    "deno": ">=1.37.2"
  },
  "packageManager": "bun@1.0.7",
  "publishConfig": {
    "access": "public"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "astro": "astro",
    "sync": "astro sync",
    "build": "astro build",
    "check": "deno task check && astro check",
    "deploy": "deployctl deploy --project=sigui --no-static --include=./dist ./dist/server/entry.mjs",
    "dev": "astro dev",
    "preview": "bun run build && bun run serve",
    "prod": "bun run build && bun run deploy",
    "serve": "deno run --allow-net --allow-read --allow-env dist/server/entry.mjs",
    "start": "astro dev --open"
  },
  "dependencies": {
    "@astrojs/check": "^0.2.1",
    "@astrojs/deno": "^5.0.1",
    "@astrojs/rss": "^3.0.0",
    "@astrojs/sitemap": "^3.0.2",
    "@astrojs/svelte": "^4.0.3",
    "@astrojs/tailwind": "^5.0.2",
    "astro": "^3.3.4",
    "svelte": "^4.2.2",
    "tailwindcss": "^3.3.4",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.8",
    "autoprefixer": "^10.4.16",
    "bun-types": "^1.0.7",
    "cssnano": "^6.0.1",
    "postcss": "^8.4.31",
    "postcss-import": "^15.1.0",
    "postcss-load-config": "^4.0.1",
    "postcss-nested": "^6.0.1",
    "ts-node": "^10.9.1",
    "tslib": "^2.6.2"
  }
}
